'use strict';

// Code here.

//keeping station names as an array, line colors as a dictionary

let line = [

redLine:['South Station','Park Street','Kendall' ,'Central','Harvard',
  'Porter','Davis','Alewife'],

greenLine:['Government Center','Park Street','Boylston','Arlington','Copley',
      'Hynes','Kenmore'],

orangeLine:['North Station','Haymarket','Park Street','State','Downtown Crossing',
      'Chinatown','Back Bay','Forest Hills'];];

let toParkStreet =[
  redLine = 1;
  greenLine = 2;
  orangeLine = 2;
];

// Nothing is edited below
const stopsBetweenStations = (startLine, startStation, endLine, endStation) => {
let beginIndex;
let endIndex;
  for (let i = 0; i < lines[startLine].length; i++) {
    let  startLine[i] = beginIndex;

    for

  )


  }
    startLine = redLine[i] + 1;
    console.log(redLine.indexOf(startLine));}


// Determine if the startStation & endStations are on the same line
  if (startLine === endLine){
    endLine - end
  }
// Not on the same line. following code sends all others to park street
  else if (startLine !== endLine){


  //  startLine
//  }



  }
};
}

module.exports = {
  stopsBetweenStations,
  stretch: false,
};
