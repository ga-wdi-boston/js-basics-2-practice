'use strict'
// Code here.
const stopsBetweenStations = (startLine, startStation, endLine, endStation) => {

}
module.exports = {
  stopsBetweenStations,
  stretch: false
}
//MY CODE STARTS here
//Instructions: The function takes the line and stop that a rider is getting on
//at and the line and stop that a rider is getting off at and
//returns the total number of stops for the trip.

//Get current position
//  Ask for input > startStation AND startLine?
//  Ask for input > endStation AND endLine?
'use strict'

const ask = require('../lib/example.js');
let answer= '';
answer = ask('Please enter your MBTA Line AND Station');


const mbtaLines = {
  Red: ['South Station', 'Park Street', 'Kendall', 'Central', 'Harvard', 'Porter', 'Davis', 'Alewife'],
  Green: ['Government Center', 'Park Street', 'Boylston', 'Arlington', 'Copley', 'Hynes', 'Kenmore'],
  Orange: ['North Station', 'Haymarket', 'Park Street', 'State', 'Downtown Crossing', 'Chinatown', 'Back Bay', 'Forest Hills']
}

while (answer === mbtaLines.Red[i])

let startLine = mbtaLines.Red
let startStation = mbtaLines.Red[0]
let endLine = mbtaLines.Red
let endStation = mbtaLines.Red[1]

const stopsBetweenStations = (startLine, startStation, endLine, endStation) => {
  var someVariable1 = function (startStation, endStation) {

  }
  var someVariable = function(startLine, endLine) {
  }
//if input === Red[something from the array] { set starting point to 0}
};
console.log(stopsBetweenStations);
//All 3 subway lines intersect at Park Street, but there are no other intersection points. Some of this MBTA is fictionalized.
//Red.getCurrentPosition();
// get the distance between [i] and [1] (or any number in the array)
// make the distance between stops return an absolute value using Math.abs()
module.exports = {
  stopsBetweenStations,
  stretch: false
}
