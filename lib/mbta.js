'use strict';

// Code here.

const stopsBetweenStations = function(startLine, startStation, endLine, endStation) {
  //let stationIndex = mbtaLine.startLine.indexOf(startStation); 
  //if (startLine === orangeLine) {
    //mbtaLine.orangeLine.indexOf(startStation) =
//  }
};
//if startLine is orangeLine, find the value
//if (startStation === 'Park Street' && startLine !== orangeLine {
// mbtaLine.startLine.indexOf(startStation) =
//
//
//}
module.exports = {
  stopsBetweenStations,
  stretch: false,
};

//lines are keys in a dictionary, values are an array

let orangeLineStops = [
   'North Station',
   'Haymarket',
   'Park Street',
   'State',
   'Downtown Crossing',
   'Chinatown',
   'Back Bay',
   'Forest Hills'
 ];
let greenLineStops = [
    'Government Center',
    'Park Street',
    'Boylston',
    'Arlington',
    'Copley',
    'Hynes',
    'Kenmore'
  ];
let redLineStops = [
    'South Station',
    'Park Street',
    'Kendall',
    'Central',
    'Harvard',
    'Porter',
    'Davis',
    'Alewife'
  ];
let mbtaLine = {
    orangeLine : orangeLineStops,
    greenLine : greenLineStops,
    redLine : redLineStops
  };

//let startStationIndex = function() {
  //mbtaLine.greenLine.indexOf()}

// make a new array of the indexes, turn the indexes into the correct distance
//depending on the lines

//let startNumber = redLineStops.indexOf(startStation)
